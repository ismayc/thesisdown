\documentclass[$doc_type$,numbers]{coppe}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,amssymb}
\usepackage{hyperref}

\makelosymbols
\makeloabbreviations

$language$

$if(portuguese)$
  \RequirePackage[english, brazil]{babel}
$endif$

$if(english)$
  \RequirePackage[brazil,english]{babel}
$endif$

$if(highlighting-macros)$
$highlighting-macros$
$endif$
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\usepackage{longtable}
\usepackage{booktabs}

\begin{document}
  \title{$title$}
  \foreigntitle{$foreigntitle$}
  $for(author)$
  \author{$author.name$}{$author.surname$}
  $endfor$

  $for(advisor)$
  \advisor{$advisor.title$}{$advisor.name$}{$advisor.surname$}{$advisor.degree$}
  $endfor$


  $for(examiner)$
  \examiner{$examiner.title$}{$examiner.name$}{$examiner.degree$}
  $endfor$

  \department{$department$}
  \date{$date_month$}{$date_year$}

  $for(keyword)$
  \keyword{$keyword$}
  $endfor$

  \maketitle

  \frontmatter
  \dedication{$dedication$}

  $for(thanks)$
  \chapter*{$thanks.title$}
  $thanks.text$
  $endfor$

  \begin{abstract}
$abstract$
  \end{abstract}

  \begin{foreignabstract}
$foreignabstract$
  \end{foreignabstract}

  \tableofcontents

$if(listoffigures)$
  \listoffigures
$endif$

$if(listoftables)$
  \listoftables
$endif$

  \printlosymbols
  \printloabbreviations

  \mainmatter

  $body$

  \backmatter
  \bibliographystyle{coppe-unsrt}
  \bibliography{thesis}

  %\appendix
  %\include{appenA}
\end{document}
